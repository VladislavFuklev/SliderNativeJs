import SwipeCarousel from "./js/swipeCarousel.js";

const carousel = new SwipeCarousel({
    // containerId: '.mySlider',
    // sliderId: '.mySlide',
    interval: 2000,
    isPlaying: true
});
carousel.init();

